asyncua
requests==2.31.0
python-decouple==3.8
influxdb>=5.3.1          # backfill InfluxDB 1.8 em modo demo

# [P0.4+P0.5+P0.6] Resiliência (opcionais — coletor degrada graciosamente se faltarem)
tenacity>=8.2.0          # retry exponencial
httpx>=0.27.0            # http async (substitui requests síncrono no event loop)
# aiohttp>=3.9.0         # fallback alternativo ao httpx (descomentar se preferir)
# aiosqlite>=0.19.0      # se quiser SQLite async nativo (atualmente usamos to_thread)
